user = render partial: "user.json.jb", locals: { user: @user }

user[:items] = @user.items.map do |gear|
  render partial: "/api/items/gear", locals: { gear: gear }
end
user
# map through items in partial and add to user show..
